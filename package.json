{
  "name": "gold-value-calculator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A fast, mobile-first intrinsic gold value calculator for SGD, INR and USD.",
  "scripts": {
    "dev": "python3 -m http.server 4173 --directory public",
    "test": "node --test",
    "check": "node --check public/src/app.js && node --check public/src/api.js && node --check public/src/calculations.js"
  }
}
